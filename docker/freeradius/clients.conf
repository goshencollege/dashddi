# Static clients are defined here; additional clients are loaded from the
# radius_client database table at startup via read_clients in the sql module.

client localhost {
    ipaddr    = 127.0.0.1
    secret    = testing123
    shortname = localhost
}
